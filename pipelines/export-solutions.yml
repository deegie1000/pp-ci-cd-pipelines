# =============================================================================
# Pipeline: Daily Power Platform Solution Export
# =============================================================================
# Runs daily at 10pm ET, or can be triggered manually on demand.
# Detects export branches for the current date, exports solutions from
# the dev environment, unpacks them, converts to managed, and creates a
# PR to main.
#
# Branch naming convention: export/yyyy-MM-dd-{token}
# Configuration file:       exports/{yyyy-MM-dd-token}/build.json
#
# Prerequisites:
#   1. Power Platform Build Tools extension installed in the ADO org
#   2. A Power Platform service connection named in the variable below
#   3. A variable group named "PowerPlatform-Dev" linked to this pipeline with:
#      - EnvironmentUrl  : Power Platform environment URL
#      - ClientId        : Service Principal Application (Client) ID
#      - ClientSecret    : Service Principal Client Secret (mark as secret)
#      - TenantId        : Azure AD Tenant ID
#      (ClientId/ClientSecret/TenantId must match the service principal backing the service connection)
#   4. Build Service account has "Contribute" and "Create Pull Request"
#      permissions on the repository
# =============================================================================

trigger: none

# -----------------------------------------------------------------------------
# Schedule: 10pm ET daily (3:00 AM UTC = 10:00 PM EST / 2:00 AM UTC = 10:00 PM EDT)
# To adjust the time, modify the cron expression below.
# Cron format: minute hour day-of-month month day-of-week (UTC)
# -----------------------------------------------------------------------------
schedules:
  - cron: "0 3 * * *"
    displayName: "Daily Export - 10pm ET"
    branches:
      include:
        - main
    always: true

# -----------------------------------------------------------------------------
# Parameters: Configurable in the ADO UI when running manually
# -----------------------------------------------------------------------------
parameters:
  - name: exportBranch
    displayName: "Override export branch (leave empty or type 'default' to auto-detect)"
    type: string
    default: ""

# -----------------------------------------------------------------------------
# Variables
# -----------------------------------------------------------------------------
variables:
  # Variable group supplying environment credentials.
  # Must contain: EnvironmentUrl, ClientId, ClientSecret (secret), TenantId
  - group: PowerPlatform-Dev

  # Name of the Power Platform service connection in ADO (used for reference;
  # pac CLI auth uses ClientId/ClientSecret/TenantId from the variable group
  # above, which must match the service principal backing this connection).
  - name: PowerPlatformServiceConnection
    value: "PowerPlatformDev"

  # Prefix applied to patch solution display names after export.
  # Used to mark old patches as obsolete so developers work on the new version.
  - name: PatchDisplayNamePrefix
    value: "(DO NOT USE) "

pool:
  vmImage: "windows-latest"

steps:
  # ===========================================================================
  # Step 1: Checkout repository with full history and persist credentials
  # ===========================================================================
  - checkout: self
    persistCredentials: true
    fetchDepth: 0

  # ===========================================================================
  # Step 2: Install Power Platform Build Tools
  # ===========================================================================
  - task: PowerPlatformToolInstaller@2
    displayName: "Install Power Platform Build Tools"

  # ===========================================================================
  # Step 3: Install pac CLI
  # ===========================================================================
  - pwsh: dotnet tool install --global Microsoft.PowerApps.CLI.Tool
    displayName: "Install pac CLI"

  # ===========================================================================
  # Step 4: Detect export branch for today's date
  # ===========================================================================
  - pwsh: |
      $ErrorActionPreference = "Stop"

      # Normalise: treat the literal word "default" the same as blank
      $exportBranch = "${{ parameters.exportBranch }}"
      if ($exportBranch -eq "default") { $exportBranch = "" }

      # Target date is always today in Eastern Time
      $eastern = [System.TimeZoneInfo]::FindSystemTimeZoneById("Eastern Standard Time")
      $targetDate = [System.TimeZoneInfo]::ConvertTimeFromUtc((Get-Date).ToUniversalTime(), $eastern).ToString("yyyy-MM-dd")
      Write-Host "Target date (Eastern): $targetDate"

      # Determine export branch
      if ($exportBranch) {
        Write-Host "Using override export branch: $exportBranch"
      } else {
        Write-Host "Auto-detecting export branch matching: export/$targetDate-*"

        $refs = git ls-remote --heads origin "refs/heads/export/$targetDate-*" 2>&1

        if ($LASTEXITCODE -ne 0 -or -not $refs) {
          Write-Host "No export branch found for date $targetDate — cancelling pipeline."
          Write-Host "##vso[task.complete result=Cancelled;]No export branch found for $targetDate"
          exit 0
        }

        # Extract branch names and sort alphabetically
        $branchNames = @($refs -split "`n" |
          Where-Object { $_.Trim() } |
          ForEach-Object { ($_ -split "refs/heads/")[1].Trim() } |
          Sort-Object)

        if ($branchNames.Count -gt 1) {
          Write-Host "##vso[task.logissue type=warning]Multiple export branches found for $targetDate. Using the first alphabetically."
          foreach ($name in $branchNames) {
            Write-Host "  Found: $name"
          }
        }

        $exportBranch = $branchNames[0]
        Write-Host "Detected export branch: $exportBranch"
      }

      # Extract subfolder name (everything after 'export/')
      $subfolder = $exportBranch -replace "^export/", ""

      Write-Host "Export branch: $exportBranch"
      Write-Host "Subfolder:     $subfolder"

      Write-Host "##vso[task.setvariable variable=ExportBranch]$exportBranch"
      Write-Host "##vso[task.setvariable variable=ExportSubfolder]$subfolder"
      Write-Host "##vso[task.setvariable variable=ExportBranchFound]true"
      Write-Host "##vso[build.updatebuildnumber]$subfolder"
      Write-Host "##vso[build.addbuildtag]$exportBranch"
    displayName: "Detect export branch"

  # ===========================================================================
  # Step 5: Checkout the export branch
  # ===========================================================================
  - pwsh: |
      $ErrorActionPreference = "Stop"

      # Record the pipeline branch HEAD before switching, so we can restore
      # scripts/ afterward. The full branch checkout overwrites all files
      # (including scripts/) with the export branch's potentially-older versions.
      $pipelineHead = git rev-parse HEAD
      Write-Host "Pipeline HEAD: $pipelineHead"

      Write-Host "Fetching export branch: $(ExportBranch)"
      git fetch origin "$(ExportBranch)"

      Write-Host "Checking out: $(ExportBranch)"
      git checkout "$(ExportBranch)"
      git pull origin "$(ExportBranch)"

      # Restore scripts/ from the pipeline branch so we always run the latest
      # version of all scripts, not whatever is on the (possibly older) export branch.
      Write-Host "Restoring scripts/ from pipeline branch ($pipelineHead)..."
      git checkout "$pipelineHead" -- "scripts/"

      Write-Host "Successfully checked out $(ExportBranch) with latest scripts"
    displayName: "Checkout export branch"
    condition: and(succeeded(), eq(variables['ExportBranchFound'], 'true'))

  # ===========================================================================
  # Step 6: Read build.json and extract solutions list
  # ===========================================================================
  - pwsh: |
      $ErrorActionPreference = "Stop"

      $buildJsonPath = Join-Path "$(Build.SourcesDirectory)" "exports/$(ExportSubfolder)/build.json"
      Write-Host "Reading build configuration from: $buildJsonPath"

      if (-not (Test-Path $buildJsonPath)) {
        Write-Error "build.json not found at: $buildJsonPath"
        exit 1
      }

      $buildConfig = Get-Content $buildJsonPath -Raw | ConvertFrom-Json
      $solutions = $buildConfig.solutions

      if (-not $solutions) { $solutions = @() }

      if ($solutions.Count -eq 0) {
        Write-Host "No solutions in build.json — config data only run."
      } else {
        Write-Host "Solutions to export ($($solutions.Count)):"
        foreach ($s in $solutions) {
          Write-Host "  - $($s.name) (v$($s.version))"
        }
      }

      # Pass solutions list as a JSON variable for subsequent steps
      $solutionsJson = if ($solutions.Count -gt 0) { @($solutions) | ConvertTo-Json -Compress } else { "[]" }
      Write-Host "##vso[task.setvariable variable=SolutionsList]$solutionsJson"
      Write-Host "##vso[task.setvariable variable=HasSolutions]$($solutions.Count -gt 0)"
    displayName: "Read build.json configuration"
    condition: and(succeeded(), eq(variables['ExportBranchFound'], 'true'))

  # ===========================================================================
  # Step 7: Authenticate pac CLI with Power Platform
  # ===========================================================================
  - pwsh: |
      $ErrorActionPreference = "Stop"

      Write-Host "Authenticating pac CLI with environment: $(EnvironmentUrl)"

      pac auth create `
        --environment "$(EnvironmentUrl)" `
        --applicationId "$(ClientId)" `
        --clientSecret "$(ClientSecret)" `
        --tenant "$(TenantId)"

      if ($LASTEXITCODE -ne 0) {
        Write-Error "Failed to authenticate with Power Platform"
        exit 1
      }

      Write-Host "Successfully authenticated with Power Platform"
      pac auth list
    displayName: "Authenticate with Power Platform"
    condition: and(succeeded(), eq(variables['ExportBranchFound'], 'true'), eq(variables['HasSolutions'], 'true'))
    env:
      ClientSecret: $(ClientSecret)

  # ===========================================================================
  # Step 8: Export solutions (unmanaged), clean unpack, and pack as managed
  # ===========================================================================
  - pwsh: |
      $ErrorActionPreference = "Stop"

      $solutions = @($env:SolutionsList | ConvertFrom-Json)
      $sourceDir = "$(Build.SourcesDirectory)"

      # Ensure output directories exist
      $unmanagedDir = Join-Path $sourceDir "solutions/unmanaged"
      $unpackedDir  = Join-Path $sourceDir "solutions/unpacked"
      $managedDir   = Join-Path $sourceDir "solutions/managed"

      New-Item -ItemType Directory -Path $unmanagedDir -Force | Out-Null
      New-Item -ItemType Directory -Path $unpackedDir  -Force | Out-Null
      New-Item -ItemType Directory -Path $managedDir   -Force | Out-Null

      $failedSolutions = @()
      $cachedSolutions = @()

      foreach ($solution in $solutions) {
        $name    = $solution.name
        $version = $solution.version

        Write-Host ""
        Write-Host "============================================"
        Write-Host "  Processing solution: $name (v$version)"
        Write-Host "============================================"

        # -----------------------------------------------------------------
        # isExisting: skip export/unpack and use pre-existing zip.
        # If isUnmanaged=true, pull from solutions/unmanaged/; otherwise managed.
        # -----------------------------------------------------------------
        if ($solution.isExisting -eq $true) {
          $isUnmanaged = $solution.PSObject.Properties["isUnmanaged"] -and [bool]$solution.isUnmanaged
          if ($isUnmanaged) {
            $existingZip = Join-Path $unmanagedDir "${name}_${version}.zip"
            $zipType = "unmanaged"
          } else {
            $existingZip = Join-Path $managedDir "${name}_${version}.zip"
            $zipType = "managed"
          }
          Write-Host "  isExisting = true — using pre-existing $zipType zip."
          if (-not (Test-Path $existingZip)) {
            Write-Error "  isExisting is true but $zipType zip not found: $existingZip"
            $failedSolutions += $name
            continue
          }
          Copy-Item -Path $existingZip -Destination "$(Build.ArtifactStagingDirectory)/${name}_${version}.zip"
          Write-Host "  Staged existing $zipType zip for artifact publishing."
          Write-Host "`n  Solution '$name' (v$version) — skipped export (isExisting=true, isUnmanaged=$isUnmanaged)."
          continue
        }

        # -----------------------------------------------------------------
        # Check if managed zip already exists for this name + version
        # -----------------------------------------------------------------
        $existingManagedZip = Join-Path $managedDir "${name}_${version}.zip"
        if (Test-Path $existingManagedZip) {
          Write-Host "  Managed zip already exists: $existingManagedZip"
          Write-Host "  Skipping export — using cached version."
          Copy-Item -Path $existingManagedZip -Destination "$(Build.ArtifactStagingDirectory)/${name}_${version}.zip"
          Write-Host "  Staged existing managed zip for artifact publishing."

          # Check unpacked source for cloud flows and patch status (already in repo from prior export)
          $cachedUnpackDir = Join-Path $unpackedDir $name

          $cachedWorkflowsDir = Join-Path $cachedUnpackDir "Workflows"
          if ((Test-Path $cachedWorkflowsDir)) {
            $cachedFlowFiles = Get-ChildItem -Path $cachedWorkflowsDir -Filter "*.json" -File -ErrorAction SilentlyContinue
            if ($cachedFlowFiles -and $cachedFlowFiles.Count -gt 0) {
              Write-Host "  Cloud flows detected (cached): $($cachedFlowFiles.Count) .json file(s)"
              $solution | Add-Member -NotePropertyName "includesCloudFlows" -NotePropertyValue $true -Force
            }
          }

          $cachedSolutionXml = Join-Path $cachedUnpackDir "Other/Solution.xml"
          if (Test-Path $cachedSolutionXml) {
            [xml]$cachedXml = Get-Content $cachedSolutionXml
            $parentNode = $cachedXml.ImportExportXml.SolutionManifest.ParentSolution
            if ($parentNode -and $parentNode.UniqueName) {
              $localizedNames = $cachedXml.ImportExportXml.SolutionManifest.LocalizedNames.LocalizedName
              $displayName = ($localizedNames | Where-Object { $_.languagecode -eq "1033" }).description
              if (-not $displayName) { $displayName = $name }
              Write-Host "  Patch solution (cached). Parent: $($parentNode.UniqueName)"
              $solution | Add-Member -NotePropertyName "isPatch" -NotePropertyValue $true -Force
              $solution | Add-Member -NotePropertyName "parentSolution" -NotePropertyValue $parentNode.UniqueName -Force
              $solution | Add-Member -NotePropertyName "displayName" -NotePropertyValue $displayName -Force
            }
          }

          Write-Host "`n  Solution '$name' (v$version) — cached, no re-export needed."
          $cachedSolutions += $name
          continue
        }

        # -----------------------------------------------------------------
        # Export unmanaged solution from Power Platform
        # -----------------------------------------------------------------
        $unmanagedZip = Join-Path $unmanagedDir "$name.zip"
        Write-Host "`nExporting unmanaged solution..."
        Write-Host "  Target: $unmanagedZip"

        pac solution export `
          --name $name `
          --path $unmanagedZip `
          --overwrite

        if ($LASTEXITCODE -ne 0) {
          Write-Error "Failed to export solution: $name"
          $failedSolutions += $name
          continue
        }
        Write-Host "  Export complete."

        # -----------------------------------------------------------------
        # Clean unpack: delete existing folder, then unpack fresh
        # -----------------------------------------------------------------
        $solutionUnpackDir = Join-Path $unpackedDir $name
        Write-Host "`nUnpacking solution (clean)..."
        Write-Host "  Target: $solutionUnpackDir"

        if (Test-Path $solutionUnpackDir) {
          Write-Host "  Removing existing unpacked folder for clean unpack..."
          Remove-Item -Path $solutionUnpackDir -Recurse -Force
        }

        pac solution unpack `
          --zipfile $unmanagedZip `
          --folder $solutionUnpackDir `
          --allowDelete true `
          --allowWrite true

        if ($LASTEXITCODE -ne 0) {
          Write-Error "Failed to unpack solution: $name"
          $failedSolutions += $name
          continue
        }
        Write-Host "  Unpack complete."

        # -----------------------------------------------------------------
        # Detect cloud flows in the unpacked solution
        # -----------------------------------------------------------------
        $workflowsDir = Join-Path $solutionUnpackDir "Workflows"
        if (Test-Path $workflowsDir) {
          $cloudFlowFiles = Get-ChildItem -Path $workflowsDir -Filter "*.json" -File -ErrorAction SilentlyContinue
          if ($cloudFlowFiles -and $cloudFlowFiles.Count -gt 0) {
            Write-Host "  Cloud flows detected: $($cloudFlowFiles.Count) .json file(s) in Workflows/"
            $solution | Add-Member -NotePropertyName "includesCloudFlows" -NotePropertyValue $true -Force
          } else {
            Write-Host "  No cloud flows detected in Workflows/"
          }
        } else {
          Write-Host "  No Workflows/ directory — no cloud flows"
        }

        # -----------------------------------------------------------------
        # Validate solution version in dev matches build.json
        # -----------------------------------------------------------------
        $solutionXmlPath = Join-Path $solutionUnpackDir "Other/Solution.xml"
        if (-not (Test-Path $solutionXmlPath)) {
          Write-Error "Solution.xml not found at: $solutionXmlPath — cannot verify version"
          $failedSolutions += $name
          continue
        }

        [xml]$solutionXml = Get-Content $solutionXmlPath
        $actualVersion = $solutionXml.ImportExportXml.SolutionManifest.Version
        Write-Host "  Version in dev environment: $actualVersion"
        Write-Host "  Version in build.json:      $version"

        if ($actualVersion -ne $version) {
          Write-Error "Version mismatch for '$name': build.json specifies v$version but dev environment has v$actualVersion. Update build.json to match the dev environment before re-running."
          $failedSolutions += $name
          continue
        }
        Write-Host "  Version check passed."

        # -----------------------------------------------------------------
        # Detect if solution is a patch (has ParentSolution element)
        # -----------------------------------------------------------------
        $parentNode = $solutionXml.ImportExportXml.SolutionManifest.ParentSolution
        if ($parentNode -and $parentNode.UniqueName) {
          $parentName = $parentNode.UniqueName
          $localizedNames = $solutionXml.ImportExportXml.SolutionManifest.LocalizedNames.LocalizedName
          $displayName = ($localizedNames | Where-Object { $_.languagecode -eq "1033" }).description
          if (-not $displayName) { $displayName = $name }
          Write-Host "  Patch solution detected."
          Write-Host "    Parent solution: $parentName"
          Write-Host "    Display name:    $displayName"
          $solution | Add-Member -NotePropertyName "isPatch" -NotePropertyValue $true -Force
          $solution | Add-Member -NotePropertyName "parentSolution" -NotePropertyValue $parentName -Force
          $solution | Add-Member -NotePropertyName "displayName" -NotePropertyValue $displayName -Force
        }

        # Rename unmanaged zip to include version
        $versionedUnmanagedZip = Join-Path $unmanagedDir "${name}_${version}.zip"
        Move-Item -Path $unmanagedZip -Destination $versionedUnmanagedZip -Force
        Write-Host "  Renamed unmanaged zip: $versionedUnmanagedZip"

        # -----------------------------------------------------------------
        # Export managed solution directly from Power Platform
        # -----------------------------------------------------------------
        $managedZip = Join-Path $managedDir "${name}_${version}.zip"
        Write-Host "`nExporting managed solution..."
        Write-Host "  Target: $managedZip"

        pac solution export `
          --name $name `
          --path $managedZip `
          --managed `
          --overwrite

        if ($LASTEXITCODE -ne 0) {
          Write-Error "Failed to export managed solution: $name"
          $failedSolutions += $name
          continue
        }
        Write-Host "  Managed export complete."

        # Copy managed zip to artifact staging directory
        Copy-Item -Path $managedZip -Destination "$(Build.ArtifactStagingDirectory)/${name}_${version}.zip"
        Write-Host "  Staged managed zip for artifact publishing."

        Write-Host "`n  Solution '$name' (v$version) processed successfully."
      }

      # -----------------------------------------------------------------
      # Write updated build.json (with includesCloudFlows flags) and stage
      # -----------------------------------------------------------------
      $buildJsonPath = Join-Path $sourceDir "exports/$(ExportSubfolder)/build.json"
      $buildConfig   = Get-Content $buildJsonPath -Raw | ConvertFrom-Json
      $updatedConfig = @{ solutions = @($solutions) }

      # Preserve configData if it exists in the original config
      if ($buildConfig.PSObject.Properties["configData"] -and $buildConfig.configData) {
        $updatedConfig["configData"] = $buildConfig.configData
      }

      $updatedConfig | ConvertTo-Json -Depth 10 | Set-Content -Path $buildJsonPath -Encoding UTF8
      Write-Host "Updated build.json with detection results."

      Write-Host ""
      Write-Host "============================================"
      Write-Host "  Processing Summary"
      Write-Host "============================================"
      $exportedCount = $solutions.Count - $failedSolutions.Count - $cachedSolutions.Count
      Write-Host "Total solutions: $($solutions.Count)"
      Write-Host "Exported:        $exportedCount"
      Write-Host "Cached:          $($cachedSolutions.Count)"
      Write-Host "Failed:          $($failedSolutions.Count)"

      if ($cachedSolutions.Count -gt 0) {
        Write-Host "Cached solutions (skipped re-export): $($cachedSolutions -join ', ')"
      }

      if ($failedSolutions.Count -gt 0) {
        Write-Host "Failed solutions: $($failedSolutions -join ', ')"
        Write-Error "One or more solutions failed to process"
        exit 1
      }

      # Signal that there are solutions ready to deploy (even if no source file changes)
      # Used by the commit step to create an empty commit for isExisting/isRollback runs.
      Write-Host "##vso[task.setvariable variable=HasSolutionsToDeploy]true"

      # Update SolutionsList variable with cloud flow flags for downstream steps
      $solutionsJson = @($solutions) | ConvertTo-Json -Compress
      Write-Host "##vso[task.setvariable variable=SolutionsList]$solutionsJson"

      Write-Host "All solutions processed successfully."
    displayName: "Export and unpack solutions"
    condition: and(succeeded(), eq(variables['ExportBranchFound'], 'true'), eq(variables['HasSolutions'], 'true'))

  # ===========================================================================
  # Step 9: Stage artifacts and set config data variables
  # ===========================================================================
  # Always runs (when export branch found) — stages build.json and deployment
  # settings files to the artifact, then reads configData from build.json so
  # subsequent steps know whether to extract config data.
  - pwsh: |
      $ErrorActionPreference = "Stop"

      $sourceDir = "$(Build.SourcesDirectory)"
      $buildJsonPath = Join-Path $sourceDir "exports/$(ExportSubfolder)/build.json"

      # Stage build.json (may have been updated by Step 8 with cloud flow flags)
      Copy-Item -Path $buildJsonPath -Destination "$(Build.ArtifactStagingDirectory)/build.json"
      Write-Host "Staged build.json for artifact publishing."

      # Stage deployment settings files (if any)
      $exportFolder = Join-Path $sourceDir "exports/$(ExportSubfolder)"
      $settingsFiles = Get-ChildItem -Path $exportFolder -Filter "deploymentSettings_*.json" -ErrorAction SilentlyContinue
      if ($settingsFiles) {
        foreach ($sf in $settingsFiles) {
          Copy-Item -Path $sf.FullName -Destination "$(Build.ArtifactStagingDirectory)/$($sf.Name)"
          Write-Host "Staged deployment settings file: $($sf.Name)"
        }
      }

      # Read build.json and set configData variables for downstream steps
      $buildConfig = Get-Content $buildJsonPath -Raw | ConvertFrom-Json
      if ($buildConfig.PSObject.Properties["configData"] -and $buildConfig.configData.Count -gt 0) {
        $configDataJson = @($buildConfig.configData) | ConvertTo-Json -Compress -Depth 10
        Write-Host "##vso[task.setvariable variable=ConfigDataList]$configDataJson"
        Write-Host "##vso[task.setvariable variable=HasConfigData]true"
        Write-Host "Config data sets found: $($buildConfig.configData.Count)"
      } else {
        Write-Host "##vso[task.setvariable variable=HasConfigData]false"
        Write-Host "No configData defined in build.json."
      }
    displayName: "Stage artifacts and set config data variables"
    condition: and(succeeded(), eq(variables['ExportBranchFound'], 'true'))

  # ===========================================================================
  # Step 10: Extract configuration data from Dev (if configData defined)
  # ===========================================================================
  - pwsh: |
      $ErrorActionPreference = "Stop"

      if ($env:HasConfigData -ne "true") {
        Write-Host "No configData in build.json — skipping extraction."
        exit 0
      }

      $configData = @($env:ConfigDataList | ConvertFrom-Json)
      $sourceDir = "$(Build.SourcesDirectory)"
      $exportDir = Join-Path $sourceDir "exports/$(ExportSubfolder)"
      $envUrl = "$(EnvironmentUrl)".TrimEnd("/")

      # Acquire OAuth token for Dataverse Web API
      $tokenUrl = "https://login.microsoftonline.com/$(TenantId)/oauth2/v2.0/token"
      $tokenBody = @{
        grant_type    = "client_credentials"
        client_id     = "$(ClientId)"
        client_secret = $env:ClientSecret
        scope         = "$envUrl/.default"
      }

      $tokenResponse = Invoke-RestMethod -Uri $tokenUrl -Method Post -Body $tokenBody
      $apiHeaders = @{
        "Authorization"    = "Bearer $($tokenResponse.access_token)"
        "OData-MaxVersion" = "4.0"
        "OData-Version"    = "4.0"
        "Content-Type"     = "application/json"
        "Accept"           = "application/json"
      }
      Write-Host "Authenticated with Dataverse API for config data extraction."

      # Run the Sync-ConfigData script in Extract mode
      # SourceDir is the export folder so dataFile paths are resolved relative to it
      $syncScript = Join-Path $sourceDir "scripts/Sync-ConfigData.ps1"
      & $syncScript `
        -Mode Extract `
        -ConfigData $configData `
        -EnvironmentUrl $envUrl `
        -ApiHeaders $apiHeaders `
        -SourceDir $exportDir

      # Stage config data files for the artifact
      foreach ($ds in $configData) {
        $dataFilePath = Join-Path $exportDir $ds.dataFile
        if (Test-Path $dataFilePath) {
          $destDir = Join-Path "$(Build.ArtifactStagingDirectory)" (Split-Path $ds.dataFile -Parent)
          New-Item -ItemType Directory -Path $destDir -Force | Out-Null
          Copy-Item -Path $dataFilePath -Destination (Join-Path "$(Build.ArtifactStagingDirectory)" $ds.dataFile)
          Write-Host "Staged config data: $($ds.dataFile)"
        }
      }
    displayName: "Extract config data from Dev"
    condition: and(succeeded(), eq(variables['ExportBranchFound'], 'true'))
    env:
      ClientSecret: $(ClientSecret)

  # ===========================================================================
  # Step 11: Publish managed solutions and build.json as pipeline artifact
  # ===========================================================================
  - task: PublishPipelineArtifact@1
    displayName: "Publish managed solution artifacts"
    condition: and(succeeded(), eq(variables['ExportBranchFound'], 'true'))
    inputs:
      targetPath: $(Build.ArtifactStagingDirectory)
      artifact: ManagedSolutions

  # ===========================================================================
  # Step 12: Merge export deployment settings into root
  # ===========================================================================
  - pwsh: |
      $ErrorActionPreference = "Stop"

      $sourceDir = "$(Build.SourcesDirectory)"
      $exportFolder = Join-Path $sourceDir "exports/$(ExportSubfolder)"
      $rootFolder = Join-Path $sourceDir "deploymentSettings"
      $mergeScript = Join-Path $sourceDir "scripts/Merge-DeploymentSettings.ps1"

      # Only run if deployment settings files exist in the export folder
      $settingsFiles = Get-ChildItem -Path $exportFolder -Filter "deploymentSettings_*.json" -ErrorAction SilentlyContinue
      if (-not $settingsFiles -or $settingsFiles.Count -eq 0) {
        Write-Host "No deployment settings files in export folder. Skipping merge."
        exit 0
      }

      & $mergeScript -ExportFolder $exportFolder -RootFolder $rootFolder
    displayName: "Merge deployment settings into root"
    condition: and(succeeded(), eq(variables['ExportBranchFound'], 'true'))

  # ===========================================================================
  # Step 13: Commit and push changes to the export branch
  # ===========================================================================
  - pwsh: |
      $ErrorActionPreference = "Stop"

      git config user.email "pipeline@dev.azure.com"
      git config user.name "Azure DevOps Pipeline"

      # Stage solution files, deployment settings, config data, and updated build.json
      git add solutions/
      git add deploymentSettings/
      git add "exports/$(ExportSubfolder)/configdata/"
      git add "exports/$(ExportSubfolder)/build.json"

      # Check for changes
      $status = git status --porcelain solutions/ deploymentSettings/ "exports/$(ExportSubfolder)/configdata/" "exports/$(ExportSubfolder)/build.json"
      if (-not $status) {
        Write-Host "No changes detected in solutions/, deploymentSettings/, exports/$(ExportSubfolder)/configdata/, or build.json."

        # If there are solutions to deploy (e.g., isExisting or isRollback with no source delta),
        # create an empty commit so a PR can be opened and the deployment pipeline is triggered.
        if ($env:HasSolutionsToDeploy -eq "true") {
          Write-Host "Solutions to deploy (isExisting/isRollback) — creating empty commit to trigger deployment PR."
          $commitMsg = "chore: trigger deployment for [$(ExportSubfolder)] (no source changes)"
          git commit --allow-empty -m "$commitMsg"
          git push origin "$(ExportBranch)"
          if ($LASTEXITCODE -ne 0) {
            Write-Error "Failed to push empty commit to $(ExportBranch)"
            exit 1
          }
          Write-Host "##vso[task.setvariable variable=HasChanges]true"
          Write-Host "Empty commit pushed to $(ExportBranch)"
          exit 0
        } else {
          Write-Host "##vso[task.setvariable variable=HasChanges]false"
          exit 0
        }
      }

      Write-Host "Changes detected:"
      git status --short solutions/ deploymentSettings/ "exports/$(ExportSubfolder)/"

      Write-Host "##vso[task.setvariable variable=HasChanges]true"

      # Commit
      $commitMsg = "chore: export Power Platform solutions [$(ExportSubfolder)]"
      git commit -m "$commitMsg"

      # Push
      git push origin "$(ExportBranch)"

      if ($LASTEXITCODE -ne 0) {
        Write-Error "Failed to push changes to $(ExportBranch)"
        exit 1
      }

      Write-Host "Changes committed and pushed to $(ExportBranch)"
    displayName: "Commit and push solution files"
    condition: and(succeeded(), eq(variables['ExportBranchFound'], 'true'))

  # ===========================================================================
  # Step 14: Create a Pull Request and auto-complete (merge) into main
  # ===========================================================================
  - pwsh: |
      $ErrorActionPreference = "Stop"

      $orgUrl   = "$(System.TeamFoundationCollectionUri)"
      $project  = "$(System.TeamProject)"
      $repoId   = "$(Build.Repository.ID)"
      $token    = $env:SYSTEM_ACCESSTOKEN

      $headers = @{
        "Authorization" = "Bearer $token"
        "Content-Type"  = "application/json"
      }

      # Build solution list for PR description
      $solutions = @($env:SolutionsList | ConvertFrom-Json)
      $solutionBullets = ($solutions | ForEach-Object { "- $($_.name) (v$($_.version))" }) -join "`n"

      # -----------------------------------------------------------------
      # Create the Pull Request
      # -----------------------------------------------------------------
      $description = (
        "## Automated Solution Export`n`n" +
        "**Branch:** ``$(ExportBranch)```n" +
        "**Date:** $(ExportSubfolder)`n`n" +
        "### Solutions Exported`n" +
        "$solutionBullets`n`n" +
        "### Contents`n" +
        "- ``solutions/unmanaged/`` - Unmanaged solution zip files`n" +
        "- ``solutions/unpacked/``  - Unpacked solution source (clean unpack)`n" +
        "- ``solutions/managed/``   - Managed solution zip files`n`n" +
        "---`n" +
        "*This PR was created automatically by the export-solutions pipeline.*"
      )

      $prBody = @{
        sourceRefName = "refs/heads/$(ExportBranch)"
        targetRefName = "refs/heads/main"
        title         = "Export Power Platform Solutions - $(ExportSubfolder)"
        description   = $description
      } | ConvertTo-Json -Depth 5

      $createUrl = "${orgUrl}${project}/_apis/git/repositories/${repoId}/pullrequests?api-version=7.1"
      Write-Host "Creating Pull Request..."

      try {
        $pr = Invoke-RestMethod -Uri $createUrl -Method Post -Headers $headers -Body $prBody
        $prId = $pr.pullRequestId
        Write-Host "Pull Request created: PR #${prId}"
        Write-Host "URL: $($pr.url)"
      } catch {
        Write-Error "Failed to create Pull Request: $_"
        exit 1
      }

      $updateUrl = "${orgUrl}${project}/_apis/git/repositories/${repoId}/pullrequests/${prId}?api-version=7.1"

      # -----------------------------------------------------------------
      # Attempt to approve the PR (with build service identity)
      # Must happen before auto-complete/merge so the PR is still active
      # -----------------------------------------------------------------
      Write-Host "`nAttempting to approve PR..."

      $approveBody = @{ vote = 10 } | ConvertTo-Json

      $reviewerUrl = "${orgUrl}${project}/_apis/git/repositories/${repoId}/pullrequests/${prId}/reviewers/$($pr.createdBy.id)?api-version=7.1"

      try {
        Invoke-RestMethod -Uri $reviewerUrl -Method Put -Headers $headers -Body $approveBody | Out-Null
        Write-Host "PR #${prId} approved."
      } catch {
        Write-Host "##vso[task.logissue type=warning]Could not auto-approve PR #${prId}: $_"
      }

      # -----------------------------------------------------------------
      # Set the PR to auto-complete with squash merge
      # -----------------------------------------------------------------
      Write-Host "`nSetting PR to auto-complete..."

      $completeBody = @{
        autoCompleteSetBy = @{
          id = $pr.createdBy.id
        }
        completionOptions = @{
          deleteSourceBranch = $true
          mergeStrategy      = "squash"
          mergeCommitMessage = "chore: export Power Platform solutions [$(ExportSubfolder)]"
        }
      } | ConvertTo-Json -Depth 5

      try {
        $updated = Invoke-RestMethod -Uri $updateUrl -Method Patch -Headers $headers -Body $completeBody
        Write-Host "PR #${prId} set to auto-complete."
      } catch {
        Write-Host "##vso[task.logissue type=warning]Could not set auto-complete on PR #${prId}. It may require manual completion: $_"
      }

      # -----------------------------------------------------------------
      # Attempt to complete the PR directly
      # -----------------------------------------------------------------
      Write-Host "`nAttempting to complete (merge) PR..."

      $mergeBody = @{
        status                = "completed"
        lastMergeSourceCommit = $pr.lastMergeSourceCommit
        completionOptions     = @{
          deleteSourceBranch = $true
          mergeStrategy      = "squash"
          mergeCommitMessage = "chore: export Power Platform solutions [$(ExportSubfolder)]"
        }
      } | ConvertTo-Json -Depth 5

      try {
        $completed = Invoke-RestMethod -Uri $updateUrl -Method Patch -Headers $headers -Body $mergeBody
        Write-Host "PR #${prId} completed and merged into main."
      } catch {
        Write-Host "##vso[task.logissue type=warning]Could not auto-complete PR #${prId}. It may require branch policies to be satisfied first: $_"
        Write-Host "The PR has been set to auto-complete and will merge once all policies are met."
      }

      # -----------------------------------------------------------------
      # Explicitly delete the source branch via refs API
      # (completionOptions.deleteSourceBranch is unreliable in some ADO versions)
      # -----------------------------------------------------------------
      Write-Host "`nDeleting source branch: $(ExportBranch)..."

      try {
        $refsUrl    = "${orgUrl}${project}/_apis/git/repositories/${repoId}/refs?filter=heads/$(ExportBranch)&api-version=7.1"
        $branchRef  = Invoke-RestMethod -Uri $refsUrl -Method Get -Headers $headers
        if ($branchRef.value -and $branchRef.value.Count -gt 0) {
          $deleteBody = @(
            @{
              name        = "refs/heads/$(ExportBranch)"
              oldObjectId = $branchRef.value[0].objectId
              newObjectId = "0000000000000000000000000000000000000000"
            }
          ) | ConvertTo-Json
          Invoke-RestMethod -Uri "${orgUrl}${project}/_apis/git/repositories/${repoId}/refs?api-version=7.1" `
            -Method Post -Headers $headers -Body $deleteBody | Out-Null
          Write-Host "Source branch deleted: $(ExportBranch)"
        } else {
          Write-Host "Branch not found (may have already been deleted): $(ExportBranch)"
        }
      } catch {
        Write-Host "##vso[task.logissue type=warning]Could not delete source branch $(ExportBranch): $_"
      }

      Write-Host "`nPull Request processing complete."
    displayName: "Create and merge Pull Request"
    condition: and(succeeded(), eq(variables['ExportBranchFound'], 'true'), eq(variables['HasChanges'], 'true'))
    env:
      SYSTEM_ACCESSTOKEN: $(System.AccessToken)

  # ===========================================================================
  # Step 15: Post-export version management (bump versions in Dev)
  # ===========================================================================
  # Runs last so a failure here does not block the PR or the commit.
  # Errors are emitted as pipeline warnings and do not fail the build.
  - pwsh: |
      $ErrorActionPreference = "Stop"

      $sourceDir = "$(Build.SourcesDirectory)"
      $buildJsonPath = Join-Path $sourceDir "exports/$(ExportSubfolder)/build.json"
      $buildConfig = Get-Content $buildJsonPath -Raw | ConvertFrom-Json
      $solutions = @($buildConfig.solutions)

      # Check whether any solution has postExportVersion defined
      $managed = @($solutions | Where-Object { $_.PSObject.Properties["postExportVersion"] -and $_.postExportVersion })
      if ($managed.Count -eq 0) {
        Write-Host "No solutions have postExportVersion defined. Skipping version management."
        exit 0
      }

      Write-Host "============================================"
      Write-Host "  Post-Export Version Management"
      Write-Host "============================================"
      Write-Host "Solutions with postExportVersion: $($managed.Count)"
      Write-Host ""

      # ---------------------------------------------------------------
      # Acquire OAuth token for Dataverse API (CloneAsPatch / CloneAsSolution)
      # ---------------------------------------------------------------
      $envUrl = "$(EnvironmentUrl)".TrimEnd("/")
      $tokenUrl = "https://login.microsoftonline.com/$(TenantId)/oauth2/v2.0/token"
      $tokenBody = @{
        grant_type    = "client_credentials"
        client_id     = "$(ClientId)"
        client_secret = $env:ClientSecret
        scope         = "$envUrl/.default"
      }

      try {
        $tokenResponse = Invoke-RestMethod -Uri $tokenUrl -Method Post -Body $tokenBody
        $apiHeaders = @{
          "Authorization"    = "Bearer $($tokenResponse.access_token)"
          "OData-MaxVersion" = "4.0"
          "OData-Version"    = "4.0"
          "Content-Type"     = "application/json"
          "Accept"           = "application/json"
        }
        Write-Host "Authenticated with Dataverse API."
      } catch {
        Write-Host "##vso[task.logissue type=warning]Post-export version management: failed to acquire Dataverse token: $($_.Exception.Message)"
        exit 0
      }

      # ---------------------------------------------------------------
      # Process each solution that has postExportVersion
      # ---------------------------------------------------------------
      $failedUpdates = @()

      foreach ($solution in $solutions) {
        $name = $solution.name

        if (-not $solution.PSObject.Properties["postExportVersion"] -or -not $solution.postExportVersion) {
          Write-Host "Skipping '$name' — no postExportVersion defined."
          continue
        }

        if ($solution.isExisting -eq $true -or $solution.isRollback -eq $true) {
          $reason = if ($solution.isExisting -eq $true) { "isExisting=true" } else { "isRollback=true" }
          Write-Host "Skipping '$name' — version management skipped ($reason)."
          continue
        }

        $postVersion    = $solution.postExportVersion
        $createNewPatch = $false
        if ($solution.PSObject.Properties["createNewPatch"]) {
          $createNewPatch = [bool]$solution.createNewPatch
        }

        Write-Host "--------------------------------------------"
        Write-Host "Solution: $name -> v$postVersion  (createNewPatch: $createNewPatch)"
        Write-Host "--------------------------------------------"

        # ---------------------------------------------------------------
        # Query Dataverse for this solution's id, friendly name, and
        # parent. Both the createNewPatch and version-bump paths need
        # this to handle the case where $name is itself a patch.
        # ---------------------------------------------------------------
        try {
          $solQuery = "$envUrl/api/data/v9.2/solutions?`$filter=uniquename eq '$name'&`$select=solutionid,friendlyname&`$expand=parentsolutionid(`$select=uniquename,friendlyname)"
          $solResult = Invoke-RestMethod -Uri $solQuery -Headers $apiHeaders
          $solRecord  = $solResult.value[0]
          $friendlyName = $solRecord.friendlyname
          $solutionId   = $solRecord.solutionid
          $isAPatch     = $solRecord.parentsolutionid -and $solRecord.parentsolutionid.uniquename
          if ($isAPatch) {
            $parentUniqueName   = $solRecord.parentsolutionid.uniquename
            $parentFriendlyName = $solRecord.parentsolutionid.friendlyname
            Write-Host "  '$name' is a patch — parent: '$parentUniqueName'"
          } else {
            $parentUniqueName   = $name
            $parentFriendlyName = $friendlyName
          }
        } catch {
          Write-Host "##vso[task.logissue type=warning]Failed to query solution details for '$name': $($_.Exception.Message)"
          $failedUpdates += $name
          continue
        }

        if ($createNewPatch) {
          # ===========================================================
          # Create a new patch from this solution at postExportVersion.
          # If this solution is itself a patch, Dataverse requires the
          # parent (base) solution as ParentSolutionUniqueName — you
          # cannot create a patch from a patch.
          # ===========================================================
          try {
            $patchDisplayName = "$parentFriendlyName (PATCH)"
            Write-Host "  Creating new patch from '$parentUniqueName' as '$patchDisplayName' at v$postVersion..."
            $cloneBody = @{
              ParentSolutionUniqueName = $parentUniqueName
              DisplayName              = $patchDisplayName
              VersionNumber            = $postVersion
            } | ConvertTo-Json

            Invoke-RestMethod -Uri "$envUrl/api/data/v9.2/CloneAsPatch" -Method Post -Headers $apiHeaders -Body $cloneBody
            Write-Host "  New patch created successfully at v$postVersion."

            # -----------------------------------------------------------
            # If the solution being superseded is itself a patch, mark it
            # with the "(DO NOT USE) " prefix so developers know to use
            # the newly created patch instead.
            # -----------------------------------------------------------
            if ($isAPatch) {
              $prefix = "$(PatchDisplayNamePrefix)"
              if (-not $friendlyName.StartsWith($prefix)) {
                $newDisplayName = "$prefix$friendlyName"
                Write-Host "  Marking old patch '$name' as obsolete: '$newDisplayName'..."
                $renameBody = @{ friendlyname = $newDisplayName } | ConvertTo-Json
                Invoke-RestMethod -Uri "$envUrl/api/data/v9.2/solutions($solutionId)" `
                  -Method Patch -Headers $apiHeaders -Body $renameBody | Out-Null
                Write-Host "  Old patch display name updated to: '$newDisplayName'"
              } else {
                Write-Host "  Old patch already has prefix — skipping rename."
              }
            }
          } catch {
            Write-Host "##vso[task.logissue type=warning]Failed to create patch from '$name': $($_.Exception.Message)"
            $failedUpdates += $name
            continue
          }

        } else {
          # ===========================================================
          # Clone the solution to postExportVersion.
          # If $name is a patch, clone the parent (base) solution
          # instead — pac solution online-version fails when a solution
          # has existing patches; CloneAsSolution avoids this.
          # ===========================================================
          $targetName         = $parentUniqueName
          $targetFriendlyName = $parentFriendlyName
          if ($isAPatch) {
            Write-Host "  '$name' is a patch — cloning parent '$targetName' to v$postVersion..."
          } else {
            Write-Host "  Cloning '$targetName' to v$postVersion..."
          }

          try {
            $cloneBody = @{
              ParentSolutionUniqueName = $targetName
              DisplayName              = $targetFriendlyName
              VersionNumber            = $postVersion
            } | ConvertTo-Json

            Invoke-RestMethod -Uri "$envUrl/api/data/v9.2/CloneAsSolution" -Method Post -Headers $apiHeaders -Body $cloneBody
            Write-Host "  Solution cloned successfully to v$postVersion."
          } catch {
            Write-Host "##vso[task.logissue type=warning]Failed to clone solution '$targetName': $($_.Exception.Message)"
            $failedUpdates += $name
            continue
          }
        }
      }

      # ---------------------------------------------------------------
      # Summary
      # ---------------------------------------------------------------
      Write-Host ""
      if ($failedUpdates.Count -gt 0) {
        Write-Host "##vso[task.logissue type=warning]Post-export version management failed for: $($failedUpdates -join ', ')"
      } else {
        Write-Host "Post-export version management complete."
      }
    displayName: "Post-export version management"
    condition: and(succeeded(), eq(variables['ExportBranchFound'], 'true'))
    continueOnError: true
    env:
      ClientSecret: $(ClientSecret)
