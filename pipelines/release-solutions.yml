# =============================================================================
# DEPRECATED — This pipeline has been replaced by two separate pipelines:
#
#   release-solutions-test.yml    — auto-triggered, deploys to Test only
#   release-solutions-promote.yml — manual, promotes to Stage and Prod
#
# Please unregister this pipeline in Azure DevOps and register the two
# replacements. See README.md § "ADO Setup — Step 6" for instructions.
# =============================================================================

trigger: none

jobs:
  - job: Deprecated
    pool:
      vmImage: "windows-latest"
    steps:
      - pwsh: |
          Write-Host ""
          Write-Host "============================================================"
          Write-Host "  This pipeline has been deprecated."
          Write-Host "  Use release-solutions-test  (auto, Test only)"
          Write-Host "  and release-solutions-promote (manual, Stage + Prod)."
          Write-Host "============================================================"
          exit 1
        displayName: "Pipeline deprecated — see README"
